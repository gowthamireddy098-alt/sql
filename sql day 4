example1
SELECT department, SUM(salary)
FROM employees

  
example2
SELECT department, SUM(salary)
FROM employees
GROUP BY department

 example3 
SELECT department, SUM(salary)
FROM employees
GROUP BY department
HAVING SUM(salary) > 100000
